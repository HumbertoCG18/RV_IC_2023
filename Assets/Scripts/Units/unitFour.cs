using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Unit_four : MonoBehaviour
{
    double onefour = 0.25;
    double twofour = 0.5;
    double threefour = 0.75;
    double fourfour = 1.0;
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
