using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class unitTen : MonoBehaviour
{
    double oneTen = 0.1;
    double twoTen = 0.2;
    double threeTen = 0.3;
    double fourTen = 0.4;
    double fiveTen = 0.5;
    double sixTen = 0.6;
    double sevenTen = 0.7;
    double eightTen = 0.8;
    double nineTen = 0.9;
    double tenTen = 1.0;
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
