using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class fraction_ : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}

/*1 problema = escolha os que representam 1/2 (50%) 

1 fileira = errado 

2 fileira = vai dar certo, se for selecionado apenas 1 bloco 

3 fileira = errado 

4 fileira = vai dar certo, se for selecionado apenas 2 bloco



Modo de verificação do problema do 1/2 
contador, que, caso o valor da fileira selecionada /2 der um numero decimal (com virgula) = false 
e caso o valor da fileira selecionada /2 der um numero inteiro = true 

Faz um script que utilize esse gaze, e mude de cor e deixe selecionado  
*/